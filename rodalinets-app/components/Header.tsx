import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

export const Header = () => {
  return (
    <View>
      <Text>Header</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
